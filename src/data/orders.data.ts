import { Order } from '@/shared/types/order';

// Inicialmente vacío para no mostrar data falsa. 
// Los pedidos se irán agregando conforme se realicen en la tienda.
export const orders: Order[] = [];
